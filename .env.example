# Beacon model URL (required for Beacon product)
VITE_BEACON_MODEL_URL=https://example.com/beacon.glb

# Plateau model URL (required for Plateau product)
VITE_PLATEAU_MODEL_URL=https://example.com/plateau.glb

# Legacy fallback — used if VITE_BEACON_MODEL_URL is not set
VITE_MODEL_URL=https://example.com/beacon.glb
